# Gradle 빌드 최적화 설정

# 병렬 빌드 활성화
org.gradle.parallel=true

# Build Cache 활성화
org.gradle.caching=true

# Configuration Cache 활성화 (Gradle 권장)
org.gradle.configuration-cache=true

# Daemon 메모리 설정
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC

# Kotlin 증분 컴파일
kotlin.incremental=true
