# Local secrets configuration
# Copy this file to application-secrets.yml and fill in your actual values
# Usage: ./gradlew bootRun -Dspring.profiles.active=secrets

slack:
  bot-token: xoxb-your-bot-token
  workspace-id: T-your-workspace-id
